Risposte Autovalutazione Homework1:

1. Per quanto riguarda la scelta delle classi concettuali dovrei fare dei miglioramenti tra cui:
- Scegliere come nomi "DescrizionePizza" e "DescrizioneProdottoAggiuntivo" per le classi concettuali che rappresentano le pizze e i prodotti aggiuntivi che possono essere scelti dal menu di una pizzeria, l'errore nel mio pensiero è stato ragionare pensando alla pizzeria fisica che produce pizze e prodotti aggiuntivi, invece di pensare ai casi d'uso e al contesto del servizio di ordini informatici che offre l'azienda PizzaDelivery.
- Aggiungere la classe Gestore per rappresentare correttamente la situazione che si crea nel caso d'uso UC3 dove il Gestore utilizza il sistema per inserire un nuovo Titolare di pizzerie.
1. Per le associazioni dovrei fare questi miglioramenti:
- L'associazione tra la classe "Cliente" e la classe "Ordine" l'avrei dovuta chiamare "ha effettuato" invece che "effettua" dato che usare il presente rende l'associazione ambigua, potrebbe farci pensare che ci serve sapere se il cliente sta effettuando attualmente l'ordine. Mentre "ha effettuato" rende meglio l'idea di un ordine che è stato mandato dal cliente e che l'azienda PizzaDelivery deve gestire.
- L'associazione tra "Pizzeria" e "Menù" l'avrei dovuta chiamare "offre" invece che "possiede" dato che è più consono come nome al contesto della Pizzeria.
- Tra la classe "VoceOrdine" e "ProdottoAggiuntivo" ho inserito un'associazione "specifica" che ha come cardinalità uno a molti, ma questo non ha senso dato che permetterebbe solamente ad una VoceOrdine di poter avere quel prodotto aggiuntivo, mentre chiaramente esso deve essere disponibile per tutte le voci dell'ordine e tutti gli ordini. Inoltre dovrei chiamare l'associazione "richiede" dato che il cliente richiede quel prodotto aggiuntivo
- L'associazione tra "VoceOrdine" e "Pizza" l'ho chiamata "è rappresentata da" erroneamente perché nel testo VoceOrdine rappresentava la pizza ordinata, ma la classe concettuale "DescrizionePizza", non è la pizza ordinata. Il nome più consono al contesto è "è relativa a" dato che la voce dell'ordine è relativa/riferita a quella descrizione della pizza scelta dal cliente.
- L'associazione tra "Titolare" e "Pizzeria" l'ho chiamata "gestisce", ma dato che la classe concettuale della pizzeria è molto più rilevante rispetto alla classe del titolare, ha più senso chiamarla "è gestita da". Inoltre la classe Titolare deve essere associata alla classe singleton "PizzaDelivery" con il nome "è partner di", perché quando all'interno del database non ci sono ancora pizzerie, il titolare deve essere connesso in qualche modo per poterne inserire nuove.
- Dato che il titolare deve essere collegato all'azienda, l'associazione tra "Pizzeria" e "PizzaDelivery" con il nome "è partner di" può direttamente essere derivata
- Per poter rappresentare l'elencazione delle 12 Pizzerie inserite più recenti è necessario inserire un'associazione ordinata tra "PizzaDelivery" e "Pizzeria" che ha come cardinalità 1 ---- 12 rispettivamente. Il nome diventerebbe "è più recente" {ordered}
- Con l'inserimento della classe Gestore devo creare anche l'associazione tra "Gestore" e "PizzaDelivery" che devo chiamare "lavora per" con cardinalità molti a uno rispettivamente.
1. Per quanto riguarda gli attributi potevo migliorare questi elementi:
- Gli attributi "numeroProgPizza" e "numeroProgProdAgg" non sono da aggiungere alla classe "VoceOrdine" ma questi due numeri progressivi sono da rappresentare tramite un'associazione ordinata tra la classe "Ordine" e la classe "VoceOrdine", lo stesso vale per l'associazione tra la classe "DescrizioneProdottoAggiuntivo" e "VoceOrdine".
- Ho inserito all'interno della classe "Ordine" l'attributo derivato di nome "/totale", l'avrei dovuta chiamare "/totaleComplessivo" per evitare ambiguità.
- Il nome dell'attributo "/subtotale" di VoceOrdine non è completamente preciso, per renderlo più chiaro avrei potuto chiamare l'attributo "/totaleParziale"
1. Per migliorare ulteriormente la soluzione consegnata, potrei prestare maggiore attenzione alla lettura e comprensione dei requisiti, così da individuare tutte le associazioni necessarie tra le classi e definirle in modo più preciso. Un altro aspetto migliorabile riguarda la scelta dei nomi, che dovrebbero essere più chiari e specifici per rappresentare correttamente i concetti del dominio.
    
    Inoltre, devo aggiustare i nomi delle associazioni e la posizione delle classi concettuali all'interno del diagramma per poter eliminare tutti i triangoli sopra le associazioni che ho inserito; penso che la loro presenza eccessiva sia causata dalla mancanza di un layout ordinato e di nomi coerenti. Anche l’organizzazione grafica complessiva del modello potrebbe essere curata meglio, ad esempio posizionando in modo più centrale le classi concettuali principali per riflettere meglio la loro rilevanza nel sistema e migliorare la leggibilità complessiva.