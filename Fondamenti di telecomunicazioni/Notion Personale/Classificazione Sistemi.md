Linearità:

y(ax1(t) + bx2(t))= ay(x1(t)) + by(x2(t))

y nel primo membro intende applicare il SISTEMA, molte volte significa solamente calcolare quello che sta all’interno della parentesi quadra

T. invarianza:

y(x(t-t0)) = y(t-t0) per ogni t0

Causalità:

f(t) <= t per ogni t